<!DOCTYPE html>
<html>
<head>
	<title>test view</title>
</head>
<body>
	{{-- @foreach($book as $data)
		@foreach($data->authors as $datas)
		<p>{{$datas->atname}}</p>
		@endforeach
	@endforeach --}}


 {{-- {{$book->authors}}<br> --}}
{{-- @foreach($book as $data)
	{{$data->atname}}
@endforeach --}}
{{-- {{$book->bkname}}
@foreach($book->authors as $data)
	{{$data->atname}}
@endforeach
 --}}
</body>
</html>
